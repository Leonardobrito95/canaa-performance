node_modules/
dist/
.env
*.local
uploads/
*.log
.DS_Store
coverage/

# Config local do Claude Code — contém histórico de permissões com
# comandos que às vezes incluem senhas/segredos em texto puro, nunca commitar.
.claude/

# Scripts e outputs de exploração pontual (podem conter dados reais de
# clientes/produção despejados durante debug — nunca commitar).
scratch_*.js
scratch_*.ts
test_result.txt
